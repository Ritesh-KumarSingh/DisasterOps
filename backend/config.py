# Tunable weights for priority scoring
WEIGHTS = {
    "severity": 0.35,
    "density": 0.25,
    "proximity": 0.25,
    "infra": 0.15,
}

# Seconds between scenario ticks
SCENARIO_TICK_INTERVAL_SECONDS = 5
